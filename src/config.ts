import path from 'path';

/** 静态资源目录 */
export const staticDir = path.resolve(__dirname, '../build');
/** rmlmapper.jar 的位置 */
export const rmlmapperPath = path.resolve(__dirname, '../bin/rmlmapper.jar');
/** 临时目录 */
export const tmpDir = path.resolve(__dirname, '../tmp');
